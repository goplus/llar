id "test/testfail"

fromVer "1.0.0"

onBuild (ctx, proj, out) => {
	out.setMetadata "-lFAIL"
}

onTest (ctx, proj, out) => {
	_, err := proj.readFile("nonexistent.txt")
	if err != nil {
		out.addErr err
	}
}
